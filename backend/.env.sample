# Application
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://pos-system-frontend.vercel.app

# JWT
JWT_SECRET=your-production-jwt-secret-minimum-32-chars
JWT_EXPIRATION=7d

# Security
BCRYPT_ROUNDS=12

# Redis (Railway will provide)
REDIS_URL=redis://railway-redis-url:6379

# Railway will automatically inject DATABASE_URL